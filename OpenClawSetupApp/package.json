{
  "name": "OpenClawSetupApp",
  "version": "1.0.0",
  "description": "A macOS GUI app for OpenClaw setup, wrapping the shell script in a user-friendly interface.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "test": "node --test",
    "prepackage-mac": "cp ../install-openclaw-stupid-simple-mac.sh ./install-openclaw-stupid-simple-mac.sh",
    "package-mac": "electron-packager . OpenClawSetupApp --platform=darwin --arch=universal --overwrite",
    "build:dmg": "npm run package-mac && mkdir -p dist && hdiutil create -volname OpenClawSetupApp -srcfolder OpenClawSetupApp-darwin-universal/OpenClawSetupApp.app -ov -format UDZO dist/OpenClawSetupApp-$npm_package_version.dmg"
  },
  "dependencies": {
    "electron": "^28.2.3"
  },
  "devDependencies": {
    "electron-packager": "^17.1.1"
  }
}
